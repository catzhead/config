# Repo-level docs — not dotfiles, must not deploy to ~/
MIGRATION.md
INVENTORY.md

# SOPS-encrypted secrets — consumed by bootstrap.sh, never deployed to ~/
secrets

# macOS-only paths
{{ if ne .chezmoi.os "darwin" -}}
.config/kitty
{{ end -}}
